import BaseScreen from './BaseScreen.tsx';
import React from 'react';

export default class LoginScreen extends BaseScreen {
  renderScreen(): React.JSX.Element {
    return super.renderScreen();
  }
}
